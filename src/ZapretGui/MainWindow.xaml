<Window x:Class="ZapretGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="zapret-gui"
        Icon="Assets/App.ico"
        Height="600"
        Width="960"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ContentBackground}"
        Foreground="{StaticResource TextPrimary}"
        FontFamily="Segoe UI">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="220" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Border Background="{StaticResource SidebarBackground}" Grid.Column="0" Padding="16">
      <StackPanel>
        <TextBlock Text="zapret-gui" FontSize="18" FontWeight="Bold" Margin="0 0 0 12" />
        <TextBlock Text="каналы" Foreground="{StaticResource TextSecondary}" Margin="0 0 0 8" />

        <Button Content="# strategies"
                HorizontalAlignment="Stretch"
                Command="{Binding SelectTabCommand}"
                CommandParameter="strategies"
                Background="#303238" />
        <Button Content="# resources"
                HorizontalAlignment="Stretch"
                Command="{Binding SelectTabCommand}"
                CommandParameter="resources"
                Background="#303238" />
        <Button Content="# diagnostics"
                HorizontalAlignment="Stretch"
                Command="{Binding SelectTabCommand}"
                CommandParameter="diagnostics"
                Background="#303238" />
        <Button Content="# settings"
                HorizontalAlignment="Stretch"
                Command="{Binding SelectTabCommand}"
                CommandParameter="settings"
                Background="#303238" />
      </StackPanel>
    </Border>

    <Border Grid.Column="1" Background="{StaticResource ContentBackground}" Padding="16">
      <ContentControl Content="{Binding CurrentView}" />
    </Border>
  </Grid>
</Window>
